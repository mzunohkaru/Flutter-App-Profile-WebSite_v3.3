name: flutter_profile_web_33
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # Network
  url_launcher:
  http:

  # UI
  simple_animations:
  supercharged:
  sidebarx:
  font_awesome_flutter:

  # 状態管理
  flutter_hooks:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # Generator
  build_runner:
  flutter_gen_runner:

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/app/
    - assets/utils/
    - assets/projects/
    - assets/architecture/
